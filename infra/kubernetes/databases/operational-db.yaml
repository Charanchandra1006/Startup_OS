apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: chief-operational-db
  namespace: chief-system
spec:
  serviceName: "chief-operational-db"
  replicas: 1
  selector:
    matchLabels:
      app: chief-operational-db
  template:
    metadata:
      labels:
        app: chief-operational-db
    spec:
      containers:
      - name: postgres
        image: postgres:15
        ports:
        - containerPort: 5432
        env:
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: chief-config
              key: OPERATIONAL_DB_NAME
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: chief-secrets
              key: OPERATIONAL_DB_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: chief-secrets
              key: OPERATIONAL_DB_PASSWORD
        volumeMounts:
        - name: operational-db-data
          mountPath: /var/lib/postgresql/data
  volumeClaimTemplates:
  - metadata:
      name: operational-db-data
    spec:
      accessModes: [ "ReadWriteOnce" ]
      resources:
        requests:
          storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: chief-operational-db
  namespace: chief-system
spec:
  selector:
    app: chief-operational-db
  ports:
  - port: 5432
    targetPort: 5432
